import {
  drawerWidth,
  transition,
  container
} from 'assets/jss/material-dashboard-react';

const appStyle = theme => ({
  wrapper: {
    height: "100vh"
  },
  mainPanel: {
    
  }
});

export default appStyle;